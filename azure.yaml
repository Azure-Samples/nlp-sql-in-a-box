name: nlpsql-in-a-box
hooks:
  postprovision:
    windows:
      shell: pwsh
      run: azd env get-values > .env
    posix:
      shell: sh
      run: azd env get-values > .env
metadata:
  template: nlp-sql-in-a-box@0.0.1-beta
